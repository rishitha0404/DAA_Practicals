A Max Heap is a complete binary tree where every parent node is greater than or equal to its children.
It allows efficient access to the maximum element and is commonly used in Heap Sort and priority queues.
Heapify takes O(log n), while building a heap takes O(n).