Quick Sort selects a pivot and partitions the array into elements smaller and larger than the pivot.
It is generally very fast but can become inefficient with a poor pivot choice.
Average time is O(n log n), while worst case is O(n²).