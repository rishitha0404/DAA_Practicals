Selection Sort repeatedly finds the smallest element and places it at the beginning of the unsorted part.
It performs a fixed number of comparisons regardless of the input order.
Its time complexity is O(n²) in best, average, and worst cases.