Merge Sort divides the array into smaller parts, sorts them, and then merges them together.
It provides consistent performance and is suitable for large datasets.
Its time complexity is O(n log n) with O(n) extra space.