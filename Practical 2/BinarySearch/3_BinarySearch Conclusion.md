Binary Search repeatedly divides a sorted array into two halves to locate the required element.
It is much faster than linear search for large sorted datasets.
Its time complexity is O(log n) and iterative implementation uses O(1) space.